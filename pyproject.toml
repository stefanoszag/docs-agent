[project]
name = "docs-agent"
version = "0.1.0"
description = "A local RAG knowledge agent that answers questions about project documentation, built with LangChain, LangGraph, pgvector, and FastAPI."
authors = [{ name = "Stefanos Zagkotas", email = "stefanoszag@gmail.com" }]
license = { text = "MIT" }
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.136.1",
    "langchain>=0.3.30",
    "langchain-anthropic>=1.4.3",
    "langchain-community>=0.3.31",
    "langchain-openai>=1.2.1",
    "langgraph>=0.6.11",
    "langgraph-checkpoint-postgres>=3.1.0",
    "langsmith>=0.8.5",
    "mcp>=1.27.2",
    "pgvector>=0.4.2",
    "psycopg2-binary>=2.9.12",
    "psycopg[binary]>=3.3.4",
    "pydantic>=2.13.4",
    "python-dotenv>=1.2.1",
    "rank-bm25>=0.2.2",
    "sentence-transformers>=5.5.1",
    "tqdm>=4.67.3",
    "uvicorn>=0.47.0",
]

[dependency-groups]
dev = [
    "httpx>=0.28.1",
    "pytest>=9.0.3",
]

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
